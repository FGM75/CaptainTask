<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaptainTask"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SavedActivity"
            android:label="Saved Activity"
            />
        <activity android:name=".RecoverPassword"
            android:label="Recover Password"/>
        <activity
            android:name=".Register"
            android:label="Registro" />
        <activity
            android:name=".ResetPassword"
            android:label="Resetear Contraseña" />
        <activity
            android:name=".NavegationMenu"
            android:label="Menú de Navegación" />
        <activity
            android:name=".SearchProducts"
            android:label="Buscar Productos" />

        <activity
            android:name=".Filter"
            android:label="Filter" />
        <activity
            android:name=".PersonalInformation"
            android:label="Información Personal" />
        <activity
            android:name=".Header"
            android:label="Header" />
        <activity
            android:name=".Footer"
            android:label="Footer" />
        <activity
            android:name=".LegalTerms"
            android:label="LegalTerms" />

        <activity
            android:name=".Contact"
            android:label="Contact" />
        <activity
            android:name=".Profile"
            android:label="Profile" />

        <activity
            android:name=".Notifications"
            android:label="Notifications" />

        <activity
            android:name=".AddObject"
            android:label="AddObject" /><activity
        android:name=".ShareList"
        android:label="ShareList" />

    </application>

</manifest>